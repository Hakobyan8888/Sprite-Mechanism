<Page
    x:Class="GridSprite.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GridSprite"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls" xmlns:customcontrols="using:GridSprite.CustomControls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <customcontrols:TableGridView 
                                      RowCount="3"
                                      ColumnCount="3"
                                      ItemWidth="{x:Bind Width, Mode=OneWay}"
                                      ItemHeight="{x:Bind Height, Mode=OneWay}"/>
    </Grid>
</Page>
